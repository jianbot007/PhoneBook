<!DOCTYPE html>
<html>
<head>
    <title>Phonebook Dashboard</title>
</head>
<body>
    <div class="container mt-5">
        <div class="card p-4 shadow-sm">
            <div class="mb-4">
                <h2 class="fw-bold">Hey, Welcome @Model.Username</h2>
                <p class="text-muted">@Model.PhoneNumber</p>
            </div>

            <div class="mb-4">
                <a href="/Home/ContactList" class="btn btn-success">Contact List</a>
            </div>

            <div>
                <h4 class="mb-3">Settings</h4>
                <div class="d-grid gap-2">
                    <a href="/Home/PasswordChange/@Model.id" class="btn btn-danger">Password Change</a>
                    <a href="/Home/EditProfile/@Model.id" class="btn btn-danger">Edit Profile</a>
                    <a href="/Home/Deactive/@Model.id" class="btn btn-danger">Deactivate Account</a>
                    <a href="/Home/Logout" class="btn btn-danger">Logout</a>
                </div>
            </div>
        </div>
    </div>


</body>
</html>
